header.header
  nav.nav.nav--tours
    a.nav__el(href='/') All tours
    form.nav__search
      button.nav__search-btn
        svg
          use(xlink:href='/img/icons.svg#icon-search')

      input.nav__search-input(type='text' placeholder='Search tours')

  a(href='/')
    .header__logo
      img(src='/img/logo-white.png' alt='Natours logo')

  nav.nav.nav--user
    a.nav__el(href='#') My bookings
    if user
      button#logout-btn.nav__el.nav__el--logout Logout
      a.nav__el(href='/account')
        img.nav__user-img(src=`/img/users/${user.photo}` alt=user.name)
        span= user.name.split(' ')[0]
    else
      a.nav__el(href='/login') Log in
      a.nav__el.nav__el--cta(href='/signup') Sign up

